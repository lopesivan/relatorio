#ANDROID_NDK={HOME}/Android/Sdk/ndk/21.4.7075529
#ANDROID_NDK={HOME}/Android/Sdk/ndk/23.0.7599858
#ANDROID_NDK={HOME}/Android/Sdk/ndk/25.0.8775105
#ANDROID_NDK={HOME}/Android/Sdk/ndk/25.1.8937393
#ANDROID_NDK={HOME}/Android/Sdk/ndk-bundle

REPO={HOME}/eletrica/repo
PROJECT_DIRECTORY="$(basename {PWD})"

## tools: ####################################################################

#- clone:
#ls $REPO| fzf | sed 's=^=git clone $REPO/='

#- bare
# (cd $REPO; git init --bare ${PROJECT_DIRECTORY}.git)
##hub remote remove origin
# git remote add origin ${REPO}/${PROJECT_DIRECTORY}.git
# git push --set-upstream origin main

#- clean
# ( d=${REPO}/${PROJECT_DIRECTORY}.git; test -d $d && rm -rf $d )
